<Page x:Class="CakeShopWPF.CheckoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CakeShopWPF"
      mc:Ignorable="d" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Checkout_Loaded"
      Title="CheckoutPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="14*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="BackBtn" Click="BackBtn_Click">Quay lại</Button>
            <Button x:Name="ConfirmCheckoutBtn" Click="ConfirmCheckoutBtn_Click">Xác nhận</Button>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="201*"/>
                <ColumnDefinition Width="199*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <DockPanel LastChildFill="False">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <Label DockPanel.Dock="Top">Thanh toán trực tiếp</Label>
                        <RadioButton x:Name="DirectBtn" Checked="DirectBtn_Checked"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <TextBlock>Tổng thu</TextBlock>
                        <TextBox x:Name="tbDirectTotalPrice" Width="50" TextChanged="tbDirectTotalPrice_TextChanged"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <TextBlock>Tiền mặt</TextBlock>
                        <TextBox Width="50" x:Name="tbCash" TextChanged="tbCash_TextChanged"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <TextBlock>Trả lại</TextBlock>
                        <TextBox Width="50" x:Name="tbReturnCash" TextChanged="tbReturnCash_TextChanged"/>
                    </StackPanel>
                </DockPanel>

            </Grid>
            <Grid Grid.Column="1">
                <DockPanel LastChildFill="False">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <Label DockPanel.Dock="Top">Giao hàng tận nơi</Label>
                        <RadioButton x:Name="DeliveryBtn" Checked="DeliveryBtn_Checked"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
                        <TextBlock>Địa chỉ</TextBlock>
                        <TextBox Width="Auto" TextChanged="tbShippingAdress_TextChanged"  x:Name="tbShippingAdress"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <TextBlock>Phí giao hàng</TextBlock>
                        <TextBox Width="50" x:Name="tbShippingFee" TextChanged="tbShippingFee_TextChanged"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                        <TextBlock>Tổng thu</TextBlock>
                        <TextBox Width="50" x:Name="tbDeliveryTotalPrice" TextChanged="tbDeliveryTotalPrice_TextChanged"></TextBox>
                    </StackPanel>

                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
